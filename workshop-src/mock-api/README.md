# Mock API

HTTP 고객 데이터 API입니다.

## Purpose

This package serves profile and account data for the customer portal sample application.

## Commands

Run from the repository root:

```powershell
npm install
npm test --workspace mock-api
npm run start:mock-api
```

## Endpoints

```text
GET /health
GET /api/profile
GET /api/accounts
```
